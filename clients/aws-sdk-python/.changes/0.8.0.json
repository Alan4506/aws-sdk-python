{
  "changes": [
    {
      "type": "dependency",
      "description": "Bump `aws-sdk-bedrock-runtime` from `==0.7.0` to `==0.8.0`."
    },
    {
      "type": "dependency",
      "description": "Bump `aws-sdk-sagemaker-runtime-http2` from `==0.7.0` to `==0.8.0`."
    },
    {
      "type": "dependency",
      "description": "Bump `aws-sdk-transcribe-streaming` from `==0.7.0` to `==0.8.0`."
    }
  ]
}
