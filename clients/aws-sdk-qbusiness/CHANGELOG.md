# Changelog

## v0.7.0

### Features
* Initial client release with support for current QBusiness operations.

### Dependencies
* Bump `smithy-core` from `~=0.5.0` to `~=0.6.0`.
* Bump `smithy-aws-core` from `~=0.6.0` to `~=0.7.0`.
