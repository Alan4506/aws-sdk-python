{
  "changes": [
    {
      "type": "feature",
      "description": "Initial client release with support for current QBusiness operations."
    },
    {
      "type": "dependency",
      "description": "Bump `smithy-core` from `~=0.5.0` to `~=0.6.0`."
    },
    {
      "type": "dependency",
      "description": "Bump `smithy-aws-core` from `~=0.6.0` to `~=0.7.0`."
    }
  ]
}
